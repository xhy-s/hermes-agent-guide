# RL 训练环境

## 8.1 概述

**核心目录**: `environments/` 和 `tools/rl_training_tool.py`

**功能**: Atropos RL 环境，支持强化学习训练和轨迹压缩。

---

## 8.2 Atropos 环境

```python
# environments/atropos/
class AtroposEnvironment:
    """
    Atropos 强化学习环境。

    用于训练 Agent 的决策能力。
    """

    def __init__(self, config: dict):
        self.config = config
        self.state = None

    def reset(self) -> dict:
        """重置环境"""
        self.state = self._init_state()
        return self.state

    def step(self, action: dict) -> tuple:
        """执行动作"""
        # 1. 执行 action
        # 2. 获取 reward
        # 3. 返回 (next_state, reward, done, info)
        pass
```

---

## 8.3 轨迹压缩

```python
# trajectory_compressor.py
class TrajectoryCompressor:
    """对话轨迹压缩"""

    def compress(self, trajectory: list) -> list:
        """
        压缩轨迹。

        保留关键决策点，去除冗余信息。
        """
        # 1. 识别关键点 (工具调用、决策分支)
        # 2. 生成摘要
        # 3. 返回压缩后的轨迹
        pass
```

---

## 8.4 关键源码索引

| 功能 | 文件 | 关键类/函数 |
|------|------|------------|
| Atropos 环境 | `environments/atropos/` | `AtroposEnvironment` |
| 轨迹压缩 | `trajectory_compressor.py` | `TrajectoryCompressor` |
| RL 训练工具 | `tools/rl_training_tool.py` | `RLTrainingTool` |

---

## 8.5 下一步

- [文件索引](../../appendix/01-file-index.md) - 核心文件映射
- [配置项参考](../../appendix/02-config-ref.md) - 配置说明

---

[← 返回总目录](../SUMMARY.md) | [← Cron](02-cron.md) | [文件索引 →](../../appendix/01-file-index.md)
